apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    componenttt: web-appp
spec:
  containers:
    - name: client
      image: stephengrider/multi-worker
      ports:
        - containerPort: 3000